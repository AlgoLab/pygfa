# AI! write a python program that receives a command line option that is the
# filename of a gfa file, reads the file using the from_gfa method and pretty
# prints the resulting graph, using the pprint method
